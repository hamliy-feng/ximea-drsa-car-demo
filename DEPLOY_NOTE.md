# 展示版本说明

当前 GitHub Pages 页面已从“买车类比”改为论文中的 MOKP 多目标背包实验案例。

使用的关键论文示例：

- 第 1 次迭代：规则较粗，121 个 Pareto 前沿解几乎全部被视为 good。
- 第 101 次迭代：good 规则收紧到 `f1 >= 3816 and f2 >= 3809`，约 14 个 PF 解保留。
- 第 576 次迭代：good 规则收紧到 `f1 >= 3828 and f2 >= 3827`，约 8 个 PF 解保留，且包含人工 DM 最优解。

该页面用于课堂可视化展示，不声称复现论文完整实验数据集。
